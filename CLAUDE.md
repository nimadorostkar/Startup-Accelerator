@AGENTS.md

## Project docs

Read `docs/README.md` before changing features or building the backend:

- `docs/pages-and-features.md` — every page, field (with form names), validation rule, status and permission.
- `docs/backend-integration.md` — contracts for the two swap points (`src/lib/auth.ts`, `src/lib/application/store.ts`), database schema, rules the backend must enforce, build order.

Update the relevant doc in the same commit as any change it describes.
